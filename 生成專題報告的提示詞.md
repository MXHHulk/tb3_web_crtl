# 生成專題報告的指令 (Prompt) 模板

這是一份專為 AI 設計的提示詞，旨在幫助您生成一份結構嚴謹、內容專業的 TurtleBot3 CCPP 專題報告。

---

## 複製以下指令並貼給 AI：

### 指令內容開始

請扮演一位「資深機器人工程師與學術論文導師」，協助我根據以下專案背景，撰寫一份高品質的**專案結案報告 (Project Report)**。

#### 1. 專案背景資訊
*   **專案名稱**：TurtleBot3 全覆蓋路徑規劃 (CCPP) 整合系統
*   **硬體平台**：TurtleBot3 Burger
*   **軟體環境**：ROS Noetic (Ubuntu 20.04), Python 3
*   **核心技術**：
    *   **SLAM**：Gmapping (即時建圖)
    *   **導航**：move_base (DWA 本地規劃器)
    *   **CCPP 演算法**：牛耕式 (Boustrophedon) 分解演算法
    *   **影像處理**：OpenCV (用於地圖分析與區域偵測)
    *   **使用者介面**：Web-based Monitor (HTML/JS/Rosbridge)
*   **專案結構**：包含 `ccpp_manager` (中樞控制)、`coverage_planner` (路徑生成)、`region_detector` (區域分析) 等模組。

#### 2. 報告需求結構
請根據以上資訊，詳細撰寫以下章節（請使用繁體中文，語氣需專業且精確）：

1.  **專案摘要 (Abstract)**：概述專案目標、解決的痛點及核心成果。
2.  **系統架構 (System Architecture)**：
    *   描述硬體與軟體的層級結構。
    *   解釋 ROS 節點間如何透過 Topic 與 Service 通訊。
3.  **核心技術詳解**：
    *   **地圖預處理**：說明如何利用 OpenCV 的閉合運算與膨脹腐蝕處理 SLAM 地圖。
    *   **區域偵測邏輯**：說明如何從二值化地圖中分離出「待清掃區域」。
    *   **牛耕式路徑生成**：詳細描述如何計算多邊形最長邊、旋轉座標系並生成 Z 字型路徑點。
4.  **系統流程描述**：
    *   請文字描述一個完整的任務流程（從網頁點擊 Start 到任務完成）。
5.  **核心邏輯優化（重點改進項目）**：
    *   **從「多邊形」到「矩形區域」的轉化**：分析為何複雜多邊形會導致牛耕式路徑看起來不夠明顯。
    *   **改進方案**：探討如何利用 OpenCV 的 `cv2.boundingRect()` 或 `cv2.minAreaRect()` 將偵測到的區域強制簡化為最大外接矩形或最小包圍矩形。
    *   **預期效益**：說明整齊的方形區域如何幫助觀察者更直觀地驗證 Z 字型路徑的平行度與重疊率，提升展示的可視化品質。
6.  **實驗成果與討論**：
    *   描述系統如何處理邊緣覆蓋。
    *   分析 CCPP 任務進度 (`task_progress`) 的計算方式。
7.  **Q&A 模擬問答**：列出 5 個專業評審可能會問的問題，並提供邏輯嚴密的回答。

#### 3. 圖表描述指令
針對報告中的流程圖與架構圖，請為我寫出對應的 **Mermaid.js** 程式碼，以便我能直接嵌入 Markdown 文件中：
*   請生成一個「系統狀態機切換 (IDLE -> EXPLORING -> COVERING)」的狀態圖。
*   請生成一個「核心邏輯運作順序」的流程圖。

---

### 指令內容結束

## 使用建議：
1. **提供程式碼細節**：如果您希望報告更具體，可以在貼上指令後，附帶貼上您的 `ccpp_manager.py` 或 `coverage_planner.py` 的內容。
2. **調整深度**：如果您是大學部專題，可以要求「淺顯易懂」；如果是研究所課程，可以要求「深入探討演算法複雜度與數學模型」。
